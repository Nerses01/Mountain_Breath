# Mountain Breath — Architecture

## System Overview

```
                        ┌──────────────────────────────────────────┐
                        │              Docker Compose              │
                        │                                          │
  Browser ──HTTPS──►  Nginx (reverse proxy)                        │
                        │   ├── /            → frontend (static React build)
                        │   └── /api/*       → backend  (Go API, port 8080)
                        │                          │
                        │                          ▼
                        │                     PostgreSQL 17
                        │                     (volume-backed)
                        └──────────────────────────────────────────┘
```

- **During development:** Vite dev server (port 5173) proxies `/api` to the Go API
  (port 8080); Postgres runs via `docker-compose.dev.yml`.
- **In production (Phase 8+):** everything is containerized behind Nginx.

## Backend Layout (Go)

Standard Go service layout, introduced in Phase 1:

```
backend/
├── cmd/api/main.go          # entry point: wiring, config, server start
├── internal/
│   ├── api/                 # HTTP layer: handlers, middleware, routing, DTOs
│   ├── domain/              # core types & business rules (no DB, no HTTP imports)
│   ├── store/               # repositories: SQL via pgx
│   └── config/              # env-based configuration
├── migrations/              # golang-migrate SQL files (NNNN_name.up/down.sql)
└── go.mod
```

Dependency direction: `api → domain ← store`. The domain package imports neither
HTTP nor SQL — this keeps business logic testable in isolation.

## Domain Model

Food & beverages catalog. Products have **variants** for weight/volume
(e.g., "Mountain Herbal Tea" → 100 g / 250 g / 500 g), each with its own price,
SKU, and stock.

```
Category 1───* Product 1───* ProductVariant
                                   │
User 1───* Order 1───* OrderItem ──┘   (references variant, snapshots price)
User 1───1 Cart  1───* CartItem ───────(references variant)
```

| Entity | Key fields | Notes |
|---|---|---|
| `Category` | id, slug, name, sort_order | flat list first; nesting only if needed |
| `Product` | id, category_id, slug, name, description, image_url, is_active | |
| `ProductVariant` | id, product_id, sku, label ("250 g" / "0.5 L"), price_minor, stock_qty | **money in integer minor units (kopecks/cents), never float** |
| `User` | id, email, password_hash, role (`customer`\|`admin`), created_at | |
| `Cart` / `CartItem` | cart: user_id or session_id; item: variant_id, qty | server-side cart |
| `Order` | id, user_id, status, total_minor, created_at | status state machine below |
| `OrderItem` | order_id, variant_id, name_snapshot, price_minor_snapshot, qty | snapshots survive later catalog edits |

**Order status state machine:**
`pending → confirmed → shipped → delivered`, with `cancelled` reachable from
`pending`/`confirmed` only. Transitions validated in the domain layer.

**Checkout invariant:** order creation runs in a single DB transaction — check stock,
decrement stock, insert order + items, or roll everything back. Guarded against
concurrent checkouts (row locking `SELECT ... FOR UPDATE`).

## API Conventions

- Base path `/api/v1`
- Plural resource nouns: `GET /api/v1/products`, `GET /api/v1/products/{slug}`,
  `POST /api/v1/cart/items`, `POST /api/v1/orders`
- JSON everywhere; errors use one envelope:
  ```json
  { "error": { "code": "validation_failed", "message": "...", "fields": { "name": "required" } } }
  ```
- List endpoints support `?page=` / `?per_page=` (offset pagination first; cursor later if needed);
  paginated responses use the envelope `{"items": [...], "page": N, "per_page": N, "total": N}`
- Auth: session cookie (`HttpOnly`, `Secure`, `SameSite=Lax`); admin endpoints under `/api/v1/admin/*`
- Versioning in the path so a future v2 can coexist

## Frontend Layout (React + TypeScript)

```
frontend/
├── src/
│   ├── api/          # typed API client + TanStack Query hooks
│   ├── components/   # reusable UI components
│   ├── pages/        # route-level components (catalog, product, cart, checkout, admin)
│   ├── auth/         # auth context, guards
│   └── main.tsx
├── index.html
└── vite.config.ts    # dev proxy: /api → localhost:8080
```

## Decisions Log

| # | Decision | Rationale | Date |
|---|---|---|---|
| 1 | Go + chi over a full framework | learn stdlib fundamentals | 2026-07-02 |
| 2 | Sessions before JWT | understand the classic model and its trade-offs first | 2026-07-02 |
| 3 | Money as integer minor units | float money is a classic bug class | 2026-07-02 |
| 4 | Server-side cart | teaches state management on the backend; works logged-out via session | 2026-07-02 |
| 5 | REST before GraphQL/gRPC | foundation first | 2026-07-02 |
| 6 | Store interfaces defined at the consumer (`api.CategoryStore`), not in `store` | Go idiom; lets tests swap in fakes without touching the store package | 2026-07-26 |
| 7 | DB errors translated to domain sentinel errors (`ErrSlugTaken`) at the store boundary | API layer maps domain errors to HTTP codes without knowing SQL details | 2026-07-26 |
| 8 | Sessions in Postgres, cookie carries raw token, DB stores its SHA-256 | leak-resistant; server-side revocation and instant role changes | 2026-07-28 |
| 9 | Cart requires login (amends #4); `PUT /cart/items` with set-semantics | avoids a parallel anonymous-token system for now; idempotent cart writes | 2026-07-29 |
| 10 | Checkout locks variant rows (`FOR UPDATE`, ordered) inside one transaction | overselling impossible under concurrency; proven with a live race test | 2026-07-29 |
| 11 | Repo stays private; no enforced branch protection | business code privacy over free protection; CI status + discipline instead | 2026-07-29 |
| 12 | Host on own hardware via classic port forwarding; Cloudflare Tunnel declined | keep TLS end-to-end (Caddy/Let's Encrypt), no third party in the traffic path; Phase 9 paused until ISP unlocks 80/443 | 2026-07-30 |
| 13 | Brand orange `#e4761f` split into a decorative fill and a darker `#b8541a` "brand ink" for anything carrying text | the design's orange measures 2.9:1 under cream text and 2.7:1 as text — both below WCAG AA's 4.5:1; fixed at token-definition time so it is never baked into a component | 2026-08-04 |
| 14 | Catalog becomes apiary-only; tea and coffee retired | the Era II design's nav, hero and benefits system assume bee products throughout — adapting the copy instead would have made half of it untrue | 2026-08-05 |
| 15 | Three languages (en default, hy, ru) via per-entity `*_translations` tables, not JSONB columns | constraints and FK safety on translated text; per-field English fallback is a `COALESCE`. Costs one migration per translatable entity and a `LEFT JOIN … ON locale = ?` per read | 2026-08-05 |
| 16 | Prices stored per market in `variant_prices` (USD + AMD), not converted from one base at a live FX rate | a shelf price is a business decision held steady, not a derived number that moves between page loads; FX kept only as fallback and for reporting | 2026-08-05 |
| 17 | URL is the single source of truth for locale; no browser language detector wired in | a detector reading `navigator.language` is a second opinion that can disagree with the address bar — the failure mode is a page whose URL says Armenian while its text says English. `useLocale` syncs i18next *from* the route, one direction | 2026-08-05 |
| 18 | English served unprefixed at `/`; only `/hy` and `/ru` carry a prefix, and the prefixes are **enumerated**, not matched with `/:locale` | matches the stated default exactly, so links written in later phases keep working. A `/:locale` param binds greedily and would treat `/cart` as a language, silently rendering the home page there | 2026-08-05 |
| 19 | Per-script font fallback by `unicode-range`, appended to the type stacks — not `:lang()` overrides | CSS resolves `font-family` per CHARACTER, so an Armenian name inside an English page renders correctly with no wrapper; and `unicode-range` gates the download, so English visitors fetch neither Noto file | 2026-08-05 |
| 20 | Per-locale `search_tsv` generated with a `CASE` over **literal** text-search configuration names | generated columns must be IMMUTABLE, which rules out `to_tsvector(locale::regconfig, …)` — the cast reads the catalog and is only STABLE. Verified against the live database before the migration was written | 2026-08-05 |
| 21 | API answers validation failures with **codes** (`slug_format`), never English prose | prose baked one language into the contract — a Russian page showed an English error under the input. The client renders the code; a fourth language needs no backend change. Codes are contract: renaming one is as breaking as renaming a JSON field | 2026-08-05 |
| 22 | Admin writes take `translations` **additively**: `name`/`description` stay required and mean English | mirrors storage exactly — those are still the parent columns the read fallback terminates at. `"en"` inside `translations` is rejected, so there is never a rule needed for which of two copies wins | 2026-08-05 |
| 23 | Variant labels are **pure measurements** ("500 g", "30 ml"); the container noun moves into the product's translatable copy | a measurement means the same thing in three languages, so `label` stays locale-invariant beside `sku` and `price_minor` and needs no fourth translation table, no extra join on every product read, and no extra input set in the admin form. Costs a little fidelity to the mock's "500 g jar" | 2026-08-10 |
| 24 | `products.badge` stores a **closed key** (`best_seller`) with a CHECK, not badge text | badges are user-facing text but a small fixed set the shop owner never invents at runtime — UI vocabulary, not content. The catalogues own the wording, the same codes-not-prose contract as #21. A CHECK rather than a Postgres ENUM because a CHECK is dropped and recreated by any migration that adds a badge | 2026-08-10 |
| 25 | Popularity is a **denormalized `products.sales_count`**, maintained inside the checkout transaction, not an aggregate over `order_items` | the aggregate reads every order line ever written to sort six products, grows without bound, and collides with pagination (GROUP BY before LIMIT). The counter costs one UPDATE inside a lock already held, and is atomic with the order it counts. Accepted cost: derived data that can drift, so the one write path is tested. Deliberately not decremented on cancellation — "most loved" measures interest over time | 2026-08-10 |
| 26 | The catalog's SQL is assembled from **Go constants** shared by the list, count and facet queries | three readers of one definition of "which products match" cannot be kept in step by a comment. Every fragment is a compile-time constant with no runtime value in it, and user input still arrives only as bound parameters — the rule against building SQL by concatenation is about user input, not about `+` | 2026-08-10 |
| 27 | Sort is a **domain-layer whitelist** (`ParseProductSort`), and the store selects a constant ORDER BY from it | `ORDER BY` cannot be a bound parameter — Postgres plans the sort at parse time, so `ORDER BY $1` sorts by the constant string. The whitelist is therefore the only thing between a query param and the planner, which makes it a security boundary and puts it in `domain` beside the other rules rather than next to the SQL | 2026-08-10 |
| 28 | A facet group's **own filter does not narrow its own counts**; every value stays listed even at 0, but a value with nothing behind it at all is dropped (`HAVING`) | otherwise clicking "Honey" leaves every other category reading 0 and the only way out of a filter is the back button. A zero caused by a filter is information; a permanently empty row is noise | 2026-08-10 |
| 29 | Shop filter state lives **entirely in the query string**, never in React state | `useState` would break the back button, shared links, reload and open-in-new-tab all at once, and would pass every click-based test while doing so. The search params ARE the state, parsed on each render, so there is no second copy to fall out of sync | 2026-08-10 |
| 30 | The API client attaches the active locale to **every translated read**, and the locale is part of every such **query key** | E1.5 shipped a trilingual API and a frontend that never asked for a language: `/hy/shop` rendered an Armenian shell around an English catalog, and nothing failed because the backend's fallback returns valid English. The query key half matters equally — a switch changes the URL but not the key, so the cached English response would be served for the Armenian page | 2026-08-10 |

| 31 | **Decision #4 — editorial collections are keyed BY LOCALE** (`product_highlights(product_id, locale, sort_order, text)`), not split into a parent row plus a translation table | decision #6's split exists to keep locale-INVARIANT fields out of per-language rows; a highlight bullet has none, so the parent would carry nothing but a `sort_order` and every read would rejoin what one row already said. Buys per-language counts (Armenian may need four bullets where English needs three); costs the guarantee that any bullet is "the translation of" another, which is right for prose and would be wrong for a product name | 2026-08-10 |
| 32 | Images keep #6's original split (`product_images` + `product_image_translations`), unlike #31 | an image row is mostly NOT text — the file, its position and whether it is the hero mean the same in every language, and only `alt` is prose. The two shapes are not inconsistent: both ask "is there a locale-invariant fact here to protect?" and get different answers | 2026-08-10 |
| 33 | One primary image per product enforced by a **partial unique index** (`… ON product_images (product_id) WHERE is_primary`), not by application code | a plain `UNIQUE (product_id, is_primary)` would also forbid two NON-primary images, which is the normal case. Consequence the store has to respect: the index rejects the intermediate state, so setting a new hero must clear the old one first, in the same transaction | 2026-08-10 |
| 34 | Related products fall back to **shared benefits then popularity**, not the planned "same category by popularity" | E2 gave the shop six products in six categories, one each, so the planned rule matches nothing and the panel would always be empty. Shared benefits is also the better claim for "often taken together" — it is about what the things do, not which shelf they sit on | 2026-08-10 |
| 35 | `GET /products/{slug}/related?curated=true` is a separate answer from the default read | the admin's picker must distinguish "curated" from "computed". Pre-filling it from the normal read would present the computed list as though it were chosen, and saving would freeze a dynamic panel into a static one; an empty picker was worse still, since one Save would wipe an existing curation | 2026-08-10 |
| 36 | Editorial collections are **replaced wholesale**, never patched row by row | the rows are keyed by position, so "the third bullet" has no identity to update once the editor reorders or deletes one. Delete-then-insert in one transaction is simpler than diffing and cannot half-apply | 2026-08-10 |

| 37 | `products.rating_avg`/`rating_count` are **recomputed from the reviews table**, in the same transaction as any change — never nudged by one review's delta | an incremental update is cheaper and drifts; an aggregate that disagrees with the rows it summarises is a bug nobody notices until a customer does. Recomputation reads only one product's reviews (bounded and small), is exact by construction, and makes the operation idempotent and order-independent — which matters because a moderator publishes, un-publishes and re-publishes | 2026-08-11 |
| 38 | A stored AVERAGE, not a running `rating_sum` + `count` | the sum/count pair is exact by construction and updates incrementally, but cannot be indexed for `ORDER BY rating_avg`, which is the entire reason the column exists. NUMERIC(3,2) rather than float: 4.65 is a number a human reads, and binary floating point cannot represent it exactly — money's rule, one step down in severity | 2026-08-11 |
| 39 | **"Most loved" keeps meaning sales, and rating is a separate sort** | rating is the more literal reading of the words and the wrong default: an average over few reviews is violently unstable, so one five-star review would outrank a jar that has sold 148 times and the front page would reshuffle on every submission. Honest star-ranking needs a Bayesian prior; the cheap half of it (tie-break by `rating_count`) is in the ORDER BY, and the expensive half is not worth it for six products | 2026-08-11 |
| 40 | Reviews default to `pending`; nothing is public until a human publishes it | the column default is what makes forgetting to moderate fail CLOSED — the worst outcome of a bug is an unpublished review, not an unmoderated one on the storefront. The public list endpoint pins `status = published` server-side rather than reading it from the query string | 2026-08-11 |
| 41 | "Verified purchase" is `EXISTS (delivered order containing a variant of this product)`, enforced in the STORE, with `can_review` on the detail response as a rendering hint only | the API's hint is not a permission — anyone can POST, so the rule is re-checked where the write happens. A non-purchaser gets 403, not 404: the product exists and the caller is known; what is missing is standing | 2026-08-11 |
| 42 | One review per person per product is a **UNIQUE constraint**, not an application check | two concurrent submissions would both pass an application-level "have you reviewed this already?" test. The store maps the 23505 violation to a domain sentinel and the API to 409 | 2026-08-11 |
| 43 | The public review list publishes a **derived display name**, never the email address | a shop that prints customers' addresses beside their opinions has built a spam list and handed it to anyone with a browser. The truncated local part is enough to say "a person, and a different one from the review above". The moderation queue does show the address — judging whether a review is genuine sometimes turns on who wrote it, and that response is behind `requireAdmin` | 2026-08-11 |

| 44 | **A price is a property of a (variant, market) PAIR** — `variant_prices` rows, per-market and set by hand, with `fx_rates` only as the fallback | a shelf price is a business decision, not a derived number: a shop picks a round figure per market and holds it. Live conversion would move the price tag between two page loads and render 6,700 ֏ as 6,743 ֏. The rate still earns its table — it prices a market the shop has not got round to, and it keeps an old order reportable | 2026-08-11 |
| 45 | `product_variants.price_minor` was **dropped**, not kept in sync | a copy of the dollar price on the variant and another in `variant_prices` is two sources of truth for one number, and they drift the first time somebody updates only one. Cost paid in the same phase: the admin editor grew a price box per market, because the column could not go until nothing wrote to it | 2026-08-11 |
| 46 | Minor units are **per currency**, so `/100` is now a bug | `minor_exponent` lives in the `currencies` table because the ROUNDING happens in SQL. USD has 2, AMD has 0 — a dram has no subdivision in circulation, so its minor unit IS the dram. `formatPrice(minor)` became `formatMoney(minor, currency)`; anything still dividing by a constant is out by two orders of magnitude in half the catalogue | 2026-08-11 |
| 47 | Totals are summed **independently in each market**, never converted from one another | rounding a sum is not the sum of roundings: three lines that each round up leave a total that disagrees with the line items a customer just added up on screen. Per-market integer prices make that impossible by construction. A market that any single line cannot be priced in is dropped from the totals entirely rather than understated — a subtotal silently missing an item is a wrong number that looks right | 2026-08-11 |
| 48 | "What does this variant cost in that currency?" is answered **once, by a SQL view** (`variant_effective_prices`), not by seven callers | the list, the count, the facet bounds, the price filter, the price sort, the cart and the checkout all ask it. It is the same instinct that made the catalog's WHERE clause a shared Go constant in E2, applied one layer down where SQL itself can hold the definition — and the `COALESCE(shelf price, converted price)` policy then exists in exactly one place | 2026-08-11 |
| 49 | Currency is **not a display concern**: it is in `ProductFilter` and changes what the store SELECTs | the price slider's bounds, the `price_asc` ordering and the `min_price`/`max_price` filter are all denominated in it, so per-market pricing means the cheapest product can genuinely differ between markets. A currency treated as formatting would have produced a correctly-shaped answer to the wrong question, silently | 2026-08-11 |
| 50 | The reads DEGRADE over a market they cannot price; the CHARGE refuses | a card with no dram price shows one line instead of two, because a shop that 500s over a missing secondary price is worse than one showing a single price. `CreateOrder` returns `ErrPriceUnavailable` → 409 in the same situation, because the alternative to failing there is billing someone zero | 2026-08-11 |
| 51 | An order carries **one currency and no second price**; a cart carries both | a cart is a live thing that can be read in either market. An order is a record of what was actually charged, and printing a converted alternative next to it invites "but you billed me the other number". `orders.fx_rate_used` rides along for reporting only, as an exact NUMERIC — and as a JSON **string**, because `JSON.parse` turns every number into a double | 2026-08-11 |
| 52 | The checkout currency is resolved at the **edge** (`?currency=` → cookie → locale guess → USD), never read from the request body | a client that could name what it pays in could name the cheaper of two markets for a basket priced in the dearer one. Same shape as `withLocale`, and it must run after it: the market guess consumes the language decision, so `?lang=hy` steers it | 2026-08-11 |
| 53 | `domain.Currencies` duplicates the `currencies` table, and a **test enforces the duplication** | the middleware must reject `?currency=ZZZ` on every request without a query, so Go needs the set; SQL needs the properties, because that is where the rounding happens. E1.5 papered over the same duplication for locales with a comment asking readers to keep three places in sync. A comment is a wish — `TestCurrenciesMatchTheDatabase` is the version that fails a build | 2026-08-11 |
| 54 | Currency lives in **localStorage + a cookie**, not in the URL, unlike the language | `/hy/shop` and `/ru/shop` are different documents: different `<html lang>`, separately shareable, separately indexable. A currency is a lens on the same document, so a path segment would double the URL space for no reader benefit and split every page's search ranking. The cookie exists because the SERVER has to see the choice at checkout | 2026-08-11 |
| 55 | `formatMoney` places the symbol itself instead of using `Intl` `style: 'currency'` | that mode takes symbol placement from the DISPLAY LOCALE, so the same 6,700 drams renders "֏6,700" for an English reader and "6 700 ֏" for an Armenian one — the price tag would change shape with the site language, which neither the design nor a shelf label does. Intl still does the hard part (grouping, decimals) against a pinned locale; `symbol_position` comes from the currency row | 2026-08-11 |

| 56 | **The order snapshots its address in flat columns; the `addresses` table is a separate, editable book** with no FK between them | third application of decision #3: prices (Phase 5), currency (E5), now the address — an order is a closed record, and nothing the customer later edits may reach into it. The book exists so the next checkout pre-fills; one default row per user, enforced by the same constant-expression partial unique index as one-primary-image (#33) and one-base-currency, upserted in the checkout's own transaction | 2026-08-14 |
| 57 | **The client sends choices, never money** — the checkout body has no money field, and `DisallowUnknownFields` turns a smuggled `total_minor` into a 400 before handler code runs | the server computes subtotal from locked shelf prices, shipping from its own rate table, tax and total from domain arithmetic. Refusing is stronger than ignoring: an ignored field invites the client to believe it worked. Card numbers are also never accepted — a stub that stores card data has bought PCI scope for nothing; the provider integration (Phase 11) takes them on the provider's servers | 2026-08-14 |
| 58 | **Totals arithmetic lives in three places on purpose**: `domain.ComputeTotals` (computed), the `orders_totals_balance` CHECK (enforced at rest), a table-driven test (documented) | the CHECK is the database's assert(): every INSERT and UPDATE proves `subtotal + shipping − discount = total`, including ones made by a future bug or someone in psql at midnight. It caught its first hand-written imbalanced INSERT (a test fixture) the day it landed | 2026-08-14 |
| 59 | **VAT is contained in the subtotal** (§1.4's Armenian retail convention), extracted as `subtotal × 20 / (100 + 20)`, integer round-half-up, and deliberately absent from the balance CHECK | "Prices include VAT" means the shelf price IS the final price; the naive `subtotal × 20%` computes the tax-added-on-top answer and overstates by a sixth. A second CHECK (`tax ≤ subtotal`) pins the containment; one domain function owns the rounding so every invoice line rounds the same way | 2026-08-14 |
| 60 | **`payment_method` and `payment_status` are two columns, not states of the order machine** | how the customer chose to pay and whether money arrived are orthogonal facts: a bank transfer is `confirmed` long before it is `paid`, and a cash order is `delivered` at the exact moment it stops being `unpaid`. Every method lands `unpaid` (card is a stub until Phase 11); the admin records the payment event | 2026-08-14 |
| 61 | **Shipping rates are rows keyed `(method, currency)`** — per-market money set by hand, no conversion fallback, read inside the checkout transaction | E5's lesson applies to fees unchanged: a delivery charge is a shelf price. The base is waived over the market's `free_over_minor`; the cold-chain surcharge never is (the mock's own cart charges $6 chilled shipping past the threshold). A market with no rate row cannot be charged at all — `ErrPriceUnavailable`, the same refuse-on-charge rule as variant prices. Not cached in Go: the whole point of a table is that the family edits it without a deploy, and a cache would turn that into "without effect until a restart" | 2026-08-14 |
| 62 | **"Cash — on delivery, AMD only" is a domain validation rule**, not UI copy | the design's own card blurb states a business fact E5 made expressible: an order knows its currency, so `ValidatePayment` rejects the pairing with a code the client can render as a fixable instruction. The UI additionally disables the option up front — but the server rule is the one that counts, because anyone can POST | 2026-08-14 |
| 63 | **The cart response quotes the checkout** — `domain.QuoteCart` for the summary card, `ComputeTotals` at charge time, both built on one `ShippingFor` | the number the customer reads before "Place the order" must be the number the shop charges, so both moments share the arithmetic; an integration test places an order and fails if the quote and the charge ever disagree. `total_minor` on the cart changed MEANING (now subtotal + shipping) because a line labelled "Total" that silently omits shipping is a lie one screen before checkout tells the truth | 2026-08-14 |
| 64 | **Someone else's order is 404, not 403** — the opposite of E4's review rule, deliberately | an order is private data: 403 confirms to an id-enumerator that order 12 exists and belongs to somebody, which is exactly what they were fishing for. E4's 403 was right because the product there is public and only the ACTION was denied. The pair documents the rule: 403 protects an action on a public thing, 404 protects the existence of a private one | 2026-08-14 |
| 65 | **The checkout renders under its own minimal chrome**, outside Layout | the mock draws it that way and the reasoning holds: the site nav is an invitation to wander off, and the one page where the shop wants no wandering is the one with the money on it. The confirmation returns to normal chrome — by then wandering is fine. The "order placed" banner rides on router state, not the URL, so a refresh or shared link shows the receipt without re-announcing a thank-you that was true once | 2026-08-14 |
| 66 | **The hive club is derived, not stored** — no `users.tier`, no memberships table; both perks are functions of `count(non-cancelled orders)` | the design's own sign-in screen defines the club as having an account ("Create an account — first order ships free", "8% less on every order after the first"), so a tier column would be a second copy of a derivable fact, and E5 (#45) showed what synced copies do: drift. The perks are mutually exclusive by construction — order one ships free, orders two onward are 8% off — and `/auth/me` serves the derived booleans so no client re-implements the rule | 2026-08-15 |
| 67 | **Promo money is per-market child rows** (`promo_code_values`), not columns on `promo_codes` | a fixed discount and a minimum-subtotal floor are MONEY, and money in this schema is a per-market shelf price (#44) — one "$5 off" converted at a rate would move with the rate. Percent and validity windows stay on the parent row because they are market-invariant. A fixed code with no amount row for the shopper's market refuses (`promo_not_in_market`) rather than converts — the same degrade/refuse rule as everything E5 touched | 2026-08-15 |
| 68 | **One pure calculator**: `domain.Price(input) → Breakdown` serves the cart page, `POST /checkout/preview` and `CreateOrder`; `QuoteCart`/`ComputeTotals` deleted, and the cart response **stopped quoting shipping and total** | three discount rules interacting (threshold, club perks, promo kinds) is past the point where "two places that happen to agree" survives. What delivery costs now depends on WHO is asking, so a cart-contents quote was one discount away from lying — the same lie #63 fixed, one phase later. The cart lists lines and their sums; the preview owns every summary figure, and the store test now proves preview-inputs and the locked charge land on the same dram | 2026-08-15 |
| 69 | **Once-per-customer is a UNIQUE index; the global cap is a count under a `FOR UPDATE` lock on the promo row** — and checkout now locks the USER row first (order: user → cart variants asc → promo → products asc) | the index makes per-user over-redemption impossible rather than unlikely (a property of storage, like #42); a global cap is a count, not a key, so it is enforced the way stock is. The user-row lock serializes one customer's parallel checkouts, closing two races in one statement: two free first deliveries, and two redemptions of a one-per-customer code. Proven by a 10-goroutine race test: one order gets a `max_redemptions = 1` code, nine are refused. Cancelling an order DELETEs its redemption — the promo equivalent of restoring stock — while the order keeps its `promo_code` text snapshot | 2026-08-15 |
| 70 | **The applied code is server-side cart state** (`cart_promos`, one row per user), judged on every read and re-judged under lock at checkout; a code that died in between is a 409 (`promo_invalid`), never a silent repricing | the customer applies on the cart page and it must survive reloads, tabs and the walk to checkout. Validity is a moment-in-time fact (codes expire, sell out, baskets shrink below floors), so it is never cached in the row — `Promo.Issue(now, …)` runs at apply (courtesy), at preview (display, with the reason named beside the code) and at charge (the rule). Refusing beats repricing because the customer approved a total the reprice would no longer match. "Unknown code" deliberately also covers disabled and not-yet-started codes — the error must not be an oracle for guessing which codes exist (the #64 reasoning, applied to a promo box) | 2026-08-15 |
| 71 | **Password reset is the sessions pattern re-armed**: raw 256-bit token to the inbox, SHA-256 to the table, a 1-hour fuse, single use enforced under `FOR UPDATE`, every session revoked on consume, previous links retired by a new request — and the request endpoint answers **204 whether or not the email exists** | a reset link is a temporary password in an inbox, so it gets a session token's leak-resistance plus expiry and single-use. Revocation closes the "changed the lock, left the window open" hole: a stolen cookie dies with the stolen password. The uniform 204 extends login's identical-errors rule — a reset endpoint that distinguished members from strangers is a free membership oracle for every address on earth; the cost (a typo waits for a mail that never comes) is stated in the UI's conditional copy | 2026-08-15 |
| 72 | **An OAuth identity is (provider, subject), never the email** — the email is read once, provider-verified, to link or mint; OAuth-born accounts get `password_hash = ''` | providers let people change email; the subject is permanent, so the account must not follow the address (a test pins exactly this). Linking on an UNVERIFIED email would let anyone claim any account by typing it at a provider. The empty hash needs no schema change and no special case: bcrypt refuses to match anything against it, so password login fails closed, and forgot-password is how such an account gains a password. Decision #5 resolved: Google real (flow hand-rolled — state cookie, server-side exchange, userinfo over our own TLS, hence no JWKS), Apple a decorative stub until the paid program is bought | 2026-08-15 |
| 73 | **Mail is a one-method interface** (`mail.Mailer`); dev delivers over REAL SMTP to Mailpit, unconfigured falls to a log sink; templates live on the backend, per-locale; the order confirmation sends after commit and is non-fatal | the store-interface pattern applied to a side effect: handlers say "deliver this", tests record it, and the provider choice (a Phase 9+ decision) changes one constructor in main. Mailpit means the dev path IS the production path, not a mock — which is how the RFC 2047 subject-encoding requirement was found (an unencoded Armenian subject is mojibake). Emails render server-side, so their copy cannot live in the browser catalogues — and `MinorExponent` joined Go under the #53 tested-duplication rule, because a receipt line needs a decimal point in the right place | 2026-08-15 |
| 74 | **Login (and forgot-password) rate limiting is a fixed-window counter per (IP, email), in memory, run BEFORE the password check** | the property needed is crude — N attempts per window — and a window counter is twenty lines with one knob; the token bucket's smoothness buys nothing here. Both key halves matter: per-account alone lets an attacker lock the owner out, per-IP alone lets a botnet through. Counting before bcrypt means guessing cannot spend the shop's hashing time. Per-process on purpose (one replica runs this shop); the caveat is written at the type: horizontal scaling multiplies the limit, and that is when it moves to storage | 2026-08-15 |
| 75 | **The wishlist's grain is the product**; "save for later" is a TRANSFER transaction (cart line out, wishlist row in, `DELETE … RETURNING` as the pivot); hearts are idempotent upserts; a retired product's heart keeps its row and leaves the list | later-you wants to remember the jar, not the jar size and count of a Tuesday — so the grain changes in transit, deliberately. A transfer is not an idempotent state write: the second identical move honestly 404s (contrast the heart itself, where PUT/DELETE state the desired truth). Keeping inactive products' rows means a returning product returns to its admirers | 2026-08-15 |
| 76 | **"Keep me signed in" is a TTL choice (7 vs 30 days), not a second cookie**, and the session token is rotated at every login | the DB row and the cookie MaxAge agree on whichever lifetime the checkbox picked, so server-side revocation keeps working identically. A separate `loginRequest` struct keeps `remember` off the register body, where `DisallowUnknownFields` rightly 400s it | 2026-08-15 |
| 77 | **Content is markdown in the repo** (decision #3 closed): one file per page per locale under `frontend/src/content/`, bundled via Vite's eager `?raw` glob, rendered by `marked` into hand-rolled `.prose` token styles; frontmatter parsed by ten lines of Go-style plain code, not a YAML dependency | zero backend, zero runtime fetch, versioned with the code — editing a page is a commit. `marked` is the second deliberate dependency (after i18next), for the same reason: a markdown parser hand-rolled badly is worse than 36 kB done right, while three `key: value` lines between fences is NOT a YAML document and needs no parser. Unsanitized `dangerouslySetInnerHTML` is recorded WITH its tripwire: content is repo-authored (its writers can already commit code), and the moment content comes from a database or a form, DOMPurify walks in | 2026-08-15 |
| 78 | **The newsletter token is a permanent capability, not a single-use secret** — the deliberate exception to the reset-token habit — and the subscription lifecycle is three timestamps, not a status enum | the same token must serve the confirm link today and the unsubscribe link at the foot of every future issue; rotating it would break the one link that must never break. Timestamps record WHEN each fact became true and cannot lie later; "live recipient" is a predicate (`confirmed AND NOT unsubscribed`), not a state to keep in sync. Corollaries pinned by the lifecycle test: a live subscriber re-subscribing gets no mail and keeps their token; a returning unsubscriber is a NEW consent proven on a fresh token | 2026-08-15 |
| 79 | **Emailed consent links land on a page whose BUTTON does a POST** — no mutating GET (the plan's original shape), and nothing fires on page load | corporate mail scanners prefetch every GET link in incoming mail, and some execute JavaScript — so both a `GET /confirm?token=` and an auto-POST-on-load page get "clicked" by robots before the human opens the message, auto-completing the very consent double opt-in exists to prove. A form submission is the one thing scanners do not do; the human pressing the button IS the consent. A test pins "zero requests on load" | 2026-08-15 |

| 80 | **Accessibility and performance are CI gates, not audits**: seven axe scans (WCAG A/AA) ride in the e2e suite, a keyboard-only purchase journey never calls `click()`, and Lighthouse runs with budgets against the production build — any of them failing fails the build | "we audited once" decays; a gate does not. The gates earned their keep on day one: six real violations (two TOKENS had to darken — ink-muted was measured against the wrong surface in E1, brand-ink was rated large-text-only and then used on body links for eight phases), a misread WCAG exemption in `Price`, a dd-before-dt `<dl>`, an opacity-halved stub, and E7's nested checkout forms. Contrast rules now measure against the WORST surface a token can sit on | 2026-08-15 |
| 81 | **Postgres JIT is disabled on the application's connections** (`jit = off` as a pool runtime param — not server-wide) | E10's k6 re-baseline found the shop's real bottleneck and it was not a query: `variant_effective_prices`' NUMERIC/`power()` math inflates cost estimates past `jit_above_cost`, so every price read JIT-compiled 49 LLVM functions (~250ms, never cached) to produce a 1ms, 4-row answer — p95 = 3,090ms under 25 users. JIT exists for analytics, where compilation amortizes over minutes-long scans; every query this shop runs is sub-millisecond OLTP. One line took p95 to 11.6ms — 17× inside the 200ms SLO. The broader lesson is recorded with it: the plan PREDICTED facets would be the bottleneck (they run 113ms p50, fine); the measurement, not the prediction, found the truth | 2026-08-15 |
| 82 | **SEO for the SPA**: a hand-rolled `usePageMeta` (title/OG/canonical/hreflang per page), JSON-LD Product+AggregateOffer+AggregateRating, a backend-generated `sitemap.xml` on the public origin, robots.txt excluding session-shaped routes; filtered shop URLs canonicalize to clean `/shop` | the helmet libraries buy SSR coordination this SPA does not do — the whole job is a handful of DOM nodes in one effect. The sitemap is the backend's because only it knows the product slugs (journal posts' slugs live in frontend markdown and are absent — a recorded cost of #77). The honesty note ships in the hook's comment: JS-managed meta reaches crawlers that render; true prerendering is Phase 11 work behind hosting. JSON-LD came LAST deliberately — E4 (real ratings) and E5 (real per-market prices) are what made its claims truthful | 2026-08-15 |
| 83 | **Two mobile collapse patterns, chosen by what they cover**: the header nav is a DISCLOSURE sheet (in flow, pushes content, no focus trap), the shop filters are a DIALOG drawer (covers the page: `role=dialog`, Escape, focus in and back out) | the difference is not style — a surface that leaves the page visible and interactive owes disclosure semantics, one that covers it owes a dialog's. Naming the rule once stops every future overlay from re-deciding it ad hoc. The sticky add-to-cart bar is the third mobile piece and deliberately NOT a new pattern: the same handler as the buy box, a second rendering of the same action | 2026-08-15 |

Add a new row whenever we make a significant technical decision.
