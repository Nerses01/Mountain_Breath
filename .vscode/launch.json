{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug API (Go)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/backend/cmd/api",
            "cwd": "${workspaceFolder}/backend",
            "env": {
                "MB_ENV": "dev",
                "MB_ADDR": ":8080"
            }
        }
    ]
}
